---
description: Stop the pet4agents desktop pet daemon.
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/scripts/pet_event.py daemon-stop)
---

Run this command exactly once to stop the pet daemon:

```bash
${CLAUDE_PLUGIN_ROOT}/scripts/pet_event.py daemon-stop
```

Then report the output to the user.
